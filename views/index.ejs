<!DOCTYPE html>
<html lang="en">
    <%- include("../views/partials/head", {title: title}); %>
    <body class="bgcolor">
        <div class="container-fluid text-center" id="post ">
            <% if ((options !== undefined) && (options.showMetadata)) { %>
            <div class="row"><h3><%= title %></h3></div>
            <div class="row"><i><%= description %></i></div>
            <% } %>
            <%- post %>

        </div>
    </body>
</html>